<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Title}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        nav {
            background-color: #3590ae;
            color: white;
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        nav a {
            color: white;
            text-decoration: none;
            margin: 0 1rem;
        }
        nav a:hover {
            text-decoration: underline;
        }
        .container {
            padding: 2rem;
            text-align: center;
        }
        .buttons {
            margin-top: 2rem;
        }
        .buttons button {
            padding: 0.5rem 1rem;
            margin: 0.5rem;
            font-size: 1rem;
            cursor: pointer;
        }
        .buttons button:hover {
            background-color: #ddd;
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo">
            <a href="/"><img src="{{.NavLogo}}" alt="Logo" style="height: 50px;"></a>
        </div>
        <div class="links">
            <a href="/interview">Start A Session</a>
            <a href="/coach/dashboard">Coach Dashboard</a>
            <a href="/about">About</a>
            {{ if .IsAuthenticated  }}
                <a href="/logout">Logout</a>
                {{ if eq .UserRole "coach" }}
                    <a href="/coach/dashboard">Dashboard</a>
                {{ end }}
                {{ if eq .UserRole "teacher" }}
                    <a href="/teacher/dashboard">Dashboard</a>
                {{ end }}
                <a href="/help">Help</a>
            {{ else }}
                <a href="/help">Help</a>
            {{ end }}
        </div>
    </nav>

    {{ block "content" . }}{{ end }}

</body>
</html>
